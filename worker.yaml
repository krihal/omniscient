checks:
  test1:
    retries: 3
    interval: 5
    check: "test1.sh"
    args: ""
    name: "Check all docker containers"
  test2:
    retries: 3
    interval: 5
    check: "test2.sh"
    args: ""
    name: "Check RADIUS port auth"

callhome:
  url: "http://localhost:8080/callhome"
  token: "foo"
